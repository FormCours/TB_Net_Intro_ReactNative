import React from 'react';
import { View, Text } from "react-native"

const TodoList = () => {

    return (
        <View style={{flex:1}}>
            <Text>Todo List</Text>
        </View>
    )
}

export default TodoList;